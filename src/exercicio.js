"use strict";
function area(numero1, numero2) {
    return numero1 * numero2;
}
;
function dizOla(nome) {
    return `Ola ${nome}`;
}
;
//arrow function
const areaArrow = (numero1, numero2) => numero1 * numero2;
const dizOlaArrow = (nome) => `Olá ${nome}`;
