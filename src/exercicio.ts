function area(numero1: number, numero2: number) : number {
    return numero1 * numero2
};

function dizOla(nome: string) : string {
    return `Ola ${nome}`
};

//arrow function
const areaArrow = (numero1:number, numero2:number) : number => numero1 * numero2;

const dizOlaArrow = (nome:string) : string => `Olá ${nome}`;